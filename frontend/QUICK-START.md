# Frontend Quick Start

1) Set the backend URL in `src/config.ts`.
2) Run the one-liner script:

```bash
./dev-frontend.sh
```

Notes:
- Requires Node.js and npm.
- Expo will open and show a QR code for Expo Go.
